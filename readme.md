

这是一个Godot 4.5.1 使用运行的项目：

作用是桌面宠物基础框架。

带有功能： 
    单线程运行
    多窗口模式
    Windows系统下隐藏任务栏（插件）
    qoi图片支持（插件）


This is a project running on Godot 4.5.1:
    It serves as a basic framework for desktop pets,with the following features:
    Single-threaded execution
    Multi-window mode
    Taskbar hiding on Windows (via plugin)
    QOI image support (via plugin)